minikube start --driver=docker
kubectl create deployment nginx --image=nginx:alpine
kubectl expose deployment nginx --type=NodePort --port=80
kubectl scale deployment nginx --replicas=4
minikube service nginx --url

kubectl create deployment mysql --image=mysql:8
kubectl set env deployment/mysql MYSQL_ROOT_PASSWORD=root
kubectl expose deployment mysql --port=3306
kubectl get pods
kubectl exec -it <pod-name> -- mysql -u root -proot

docker pull jasonrivers/nagios:latest
docker run --name nagiosdemo -p 8888:80 jasonrivers/nagios:latest
minikube dashboard




pipeline {
    agent any

    tools {
        maven 'MAVEN-HOME'
        jdk 'JDK-17'
    }

    stages {

        stage('Checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/PKarthikyadav/frost.git'
            }
        }

        stage('Clean') {
            steps {
                bat 'mvn clean'
            }
        }

        stage('Install') {
            steps {
                bat 'mvn install -DskipTests'
            }
        }

        stage('Test') {
            steps {
                bat 'mvn test'
            }
        }
    }
}




 Step 1: Open Jenkins (localhost:8080)
   	 ├── Click on "New Item" (left side menu
Step 2: Create Freestyle Project (e.g., MavenJava_Build)
        	├── Enter project name (e.g., MavenJava_Build)
        	├── Click "OK"
└── Configure the project:
            		├── Description: "Java Build demo"
            		├── Source Code Management:
            			└── Git repository URL: [GitMavenJava repo URL]
            		├── Branches to build: */Main   or  */master
  		└── Build Steps:
               	     ├── Add Build Step -> "Invoke top-level Maven targets"
                  		└── Maven version: MAVEN_HOME
                 		└── Goals: clean
                	├── Add Build Step -> "Invoke top-level Maven targets"
                		└── Maven version: MAVEN_HOME
                		└── Goals: install
                	└── Post-build Actions:
                    	       ├── Add Post Build Action -> "Archive the artifacts"
                    			└── Files to archive: **/*
                    	     ├── Add Post Build Action -> "Build other projects"
                    			└── Projects to build: MavenJava_Test
                    			└── Trigger: Only if build is stable
                    	     └── Apply and Save


    └── Step 3: Create Freestyle Project (e.g., MavenJava_Test)
        	├── Enter project name (e.g., MavenJava_Test)
        	├── Click "OK"
              └── Configure the project:
             ├── Description: "Test demo"
             ├── Build Environment:
            		└── Check: "Delete the workspace before build starts"
            ├── Add Build Step -> "Copy artifacts from another project"
            		└── Project name: MavenJava_Build
            		└── Build: Stable build only  // tick at this
            		└── Artifacts to copy: **/*
            ├── Add Build Step -> "Invoke top-level Maven targets"
            		└── Maven version: MAVEN_HOME
            		└── Goals: test
            		└── Post-build Actions:
                ├── Add Post Build Action -> "Archive the artifacts"
                	└── Files to archive: **/*
                	└── Apply and Save

    └── Step 4: Create Pipeline View for Maven Java project
        ├── Click "+" beside "All" on the dashboard
        ├── Enter name: MavenJava_Pipeline
        ├── Select "Build pipeline view"         // tick here
         |--- create
        └── Pipeline Flow:
            ├── Layout: Based on upstream/downstream relationship
            ├── Initial job: MavenJava_Build
            └── Apply and Save OK

    └── Step 5: Run the Pipeline and Check Output
        ├── Click on the trigger to run the pipeline
        ├── click on the small black box to open the console to check if the build is success
            Note : 
1.	If build is success and the test project is also automatically triggered with name       
                      “MavenJava_Test”
2.	The pipeline is successful if it is in green color as shown ->check the console of the test project
3.	The test project is successful and all the artifacts are archived successfully


└── Step 4: Create Freestyle Project (e.g., MavenWeb_Deploy)
        ├── Enter project name (e.g., MavenWeb_Deploy)
        ├── Click "OK"
        └── Configure the project:
            ├── Description: "Web Code Deployment"
            ├── Build Environment:
            		└── Check: "Delete the workspace before build starts"
            ├── Add Build Step -> "Copy artifacts from another project"
            		└── Project name: MavenWeb_Test
            		└── Build: Stable build only
               	└── Artifacts to copy: **/*
            └── Post-build Actions:
                ├── Add Post Build Action -> "Deploy WAR/EAR to a container"
   └── WAR/EAR File: **/*.war
   └── Context path: Webpath
 └── Add container -> Tomcat 9.x remote
└── Credentials: Username: admin, Password: 1234
── Tomcat URL: https://localhost:8085/
                └── Apply and Save




| **Git Command**                        | **Use / Meaning**                  | **Use Case**                         |
| -------------------------------------- | ---------------------------------- | ------------------------------------ |
| `git init`                             | Initializes a local Git repository | Start version control for a project  |
| `git add .`                            | Stage all changed files            | Prepare files for commit             |
| `git add filename`                     | Stage a specific file              | Add only selected files to commit    |
| `git reset HEAD filename`              | Unstage a file (keep changes)      | You added a file by mistake          |
| `git checkout -- filename`             | Discard unstaged changes           | Undo local changes                   |
| `git commit -m "message"`              | Commit staged changes              | Create a version of your work        |
| `git commit --amend`                   | Fix last commit message            | Correct wrong commit text            |
| `git log`                              | Shows commit history               | View past commits                    |
| `git log --oneline --graph --decorate` | Visual readable commit history     | Understand branches & merges         |
| `git config --global user.name ""`     | Set global username                | Identity for all repositories        |
| `git config --global user.email ""`    | Set global email                   | Identity for all repositories        |
| `git diff`                             | View unstaged changes              | Check what is modified               |
| `git checkout branchname`              | Switch to a branch                 | Move to feature branch               |
| `git checkout -b branchname`           | Create + switch to new branch      | Start new feature                    |
| `git branch -d branchname`             | Delete merged branch               | Cleanup                              |
| `git branch -D branchname`             | Force delete unmerged branch       | Remove unwanted branch               |
| `git branch`                           | List local branches                | View available branches              |
| `git branch -a`                        | List all local + remote branches   | Check remote branches                |
| `git reflog`                           | Shows branch history               | Recover deleted branch               |
| `git push`                             | Upload commits                     | Send work to GitHub                  |
| `git push -u origin main`              | First push (set upstream)          | Connect local & remote branch        |
| `git fetch`                            | Download changes (no merge)        | Check updates without affecting code |
| `git pull`                             | Fetch + merge                      | Update branch                        |
| `git merge branchname`                 | Merge branch into current          | Combine feature branch into main     |
| `git stash`                            | Save changes temporarily           | Switch branches safely               |
| `git stash pop`                        | Restore stashed changes            | Get back temporary work              |
| `git blame filename`                   | Show who edited each line          | Debugging / track changes            |
| `git rm filename`                      | Remove file & track removal        | Delete an unwanted file              |
| `git remote -v`                        | Show remotes                       | Check GitHub linked repo             |
| `git remote add origin url`            | Add remote repository              | Connect project to GitHub            |
| `git remote remove origin`             | Remove remote link                 | Disconnect GitHub repo               |
| `git remote rename origin newname`     | Rename remote                      | Change origin name                   |
| `git remote set-url origin url`        | Change remote URL                  | Update after renaming GitHub repo    |
| `git branch -r`                        | List remote-only branches          | See branches on GitHub               |
| `git fetch --prune`                    | Clean deleted remote branches      | Remove outdated remotes              |
| `git fetch origin branchname`          | Fetch a specific branch            | Download a single branch             |
| `git remote show origin`               | Detailed remote info               | View remote branch status            |
| `git pull --rebase origin main`        | Rebase local branch on remote      | Keep linear history                  |
























version: "3.9"

services:
  nginx:
    image: nginx:alpine
    container_name: nginx_server
    ports:
      - "8080:80"
    volumes:
      - ./html:/usr/share/nginx/html
    depends_on:
      - redis
    restart: always

  redis:
    image: redis:latest
    container_name: redis_server
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: always

volumes:
  redis_data:



docker-compose up -d
docker ps
docker exec -it redis_server redis-cli
